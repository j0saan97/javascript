los problemas específicos que evita la herencia y cómo se implementa en la práctica con JavaScript.

🔬 La Composición en Profundidad: Principios y Problemas de la Herencia
La recomendación de usar Composición sobre Herencia (conocida como el principio "Favor Composition Over Inheritance") es un pilar del diseño orientado a objetos. El punto clave de esta preferencia es evitar la fragilidad del código que introduce la herencia.

1. El Problema del Acoplamiento y la Fragilidad
Amplías correctamente que la herencia genera un acoplamiento fuerte, pero debemos entender la implicación: la fragilidad del código base.

Acoplamiento de Implementación: Cuando la clase A hereda de la clase B, la clase A no solo obtiene la interfaz (los métodos públicos) de B, sino también toda su implementación interna (los campos privados, la lógica interna de los métodos, etc.). Esto se llama Acoplamiento Fuerte.

Consecuencia (Fragilidad): Si un desarrollador modifica un método interno de la clase base B (incluso si parece ser un cambio "seguro" o privado), podría inadvertidamente romper el comportamiento de la subclase A o sus nietas, sin que A sea modificada directamente. Este es el efecto "cambio en cascada" en las jerarquías profundas.

Composición (Acoplamiento de Interfaz): En la composición, la clase C contiene una instancia del módulo D. La clase C solo interactúa con D a través de su interfaz pública bien definida. C no necesita saber cómo D implementa su lógica internamente.

Consecuencia (Robustez): Podemos cambiar la implementación interna del módulo D cuantas veces queramos, siempre y cuando mantengamos la interfaz pública intacta. Esto aísla el cambio y hace el código más robusto.

2. El Problema del "LSP" y la Relevancia del Contexto
La herencia, mal utilizada, viola fácilmente el Principio de Sustitución de Liskov (LSP), una de las "S" de S.O.L.I.D.

Violación de LSP: El LSP establece que los objetos de una subclase deben poder ser reemplazados por los objetos de su superclase sin alterar la corrección del programa.

Ejemplo clásico (LSP): Un cuadrado es un rectángulo. Si se modela con herencia (Cuadrado hereda de Rectángulo), al llamar al método establecerAlto(10) en un Cuadrado, este debe automáticamente establecer ancho=10 para mantener la propiedad de cuadrado, lo cual rompe el comportamiento esperado del método establecerAlto heredado de Rectángulo. La herencia modela la relación "es-un" (is-a) de forma demasiado estricta.

Composición (Evita el LSP): La composición modela la relación "tiene-un" (has-a). En lugar de decir que un Pájaro es un Animal, decimos que un Pájaro tiene un comportamiento de Volar y tiene un comportamiento de Caminar. Esto separa la funcionalidad de la identidad del objeto y evita conflictos de comportamiento.

3. La Rigidez del Comportamiento (Rigididad Lateral)
Tu teoría menciona que la composición facilita la extensión. Esta facilidad es crítica cuando se necesitan comportamientos opcionales o dinámicos.

Rigidez de la Herencia: Si usas herencia, un objeto hereda todos los comportamientos de su clase base, incluso aquellos que no necesita. Si necesitas que un objeto Vehiculo pueda Volar ocasionalmente, tienes dos opciones deficientes:

Crear una jerarquía compleja (VehiculoTerrestre, VehiculoAereo).

Hacer que el método Volar() de la clase base lance una excepción (Error: No puede volar) o devuelva null, lo cual obliga al cliente del código a implementar comprobaciones excesivas (if (vehiculo.puedeVolar) { vehiculo.volar() }).

Flexibilidad de la Composición: Con la composición, simplemente agregas el componente ComportamientoDeVuelo solo a los objetos que lo necesitan, quizás en tiempo de ejecución.

4. Implementación en JavaScript: Funciones Factoría y Mixins
En JavaScript, la composición se implementa de manera muy natural usando el paradigma funcional con Funciones Factoría o con Mixins.

Mixins/Composables (Patrón Decorator Estructural):

Son funciones que toman un objeto como argumento, le añaden propiedades y métodos (comportamiento) y luego devuelven el objeto modificado.

El objeto final se construye al "mezclar" o componer varios mixins.